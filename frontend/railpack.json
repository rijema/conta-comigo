{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20"
  },
  "deploy": {
    "startCommand": "npm start -- --hostname 0.0.0.0 --port ${PORT:-3000}"
  }
}
